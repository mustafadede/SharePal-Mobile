import { View, Text } from "react-native";
import React from "react";

const Line = () => {
  return (
    <View
      className="mx-2 mt-4 border-slate-200"
      style={{
        borderWidth: 0.4,
      }}
    ></View>
  );
};

export default Line;
